apiVersion: v1
kind: Secret
metadata:
  name: careconnect-secrets
  namespace: careconnect
type: Opaque
data:
  # Base64 encoded values - replace with actual encoded values
  gmail-app-password: ""  # echo -n "your-app-password" | base64
  client-id: ""           # echo -n "your-client-id" | base64  
  client-secret: ""       # echo -n "your-client-secret" | base64
  refresh-token: ""       # echo -n "your-refresh-token" | base64
